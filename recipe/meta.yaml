{% set name = "aqme" %}
{% set version = "1.5.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/aqme-{{ version }}.tar.gz
  sha256: 4db870a705a689b2ae027122d991ef6c7724f340610c0046eba12e699f2b03d2

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - ase
    - cclib
    - cffi
    - matplotlib-base
    - numpy
    - pandas
    - progress
    - python >=3.6
    - pyyaml
    - seaborn
    - openbabel
    - rdkit
    - goodvibes
    - py3Dmol
    - ipywidgets
    - dbstep

test:
  imports:
    - aqme
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/jvalegre/aqme
  summary: Automated Quantum Mechanical Environments
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jvalegre
    - shreesowndarya
